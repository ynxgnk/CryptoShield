//
//  CryptoDetailsViewController.swift
//  CryptoShield
//
//  Created by Nazar Kopeika on 04.06.2023.
//

/*

import UIKit

class CryptoDetailsViewController: UIViewController {
    
    private let nameLabel: UILabel = {
        let label = UILabel()
        label.font = .systemFont(ofSize: 18, weight: .bold)
        label.numberOfLines = 1
        label.textAlignment = .center
        label.text = "Ethereum"
        label.textColor = .white
        return label
    }()
    
    private let symbolLabel: UILabel = {
        let label = UILabel()
        label.font = .systemFont(ofSize: 14, weight: .semibold)
        label.numberOfLines = 1
        label.text = "ETH"
        label.textColor = .white
        return label
    }()
    
    private let valueLabel: UILabel = {
       let label = UILabel()
        label.font = .systemFont(ofSize: 18, weight: .regular)
        label.numberOfLines = 1
        label.text = "1800$"
        label.textColor = .white
        return label
    }()
    
//    private let cryptoChart: ChartData
    
    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .systemBackground
        view.addSubview(nameLabel)
        nameLabel.backgroundColor = .red
        view.addSubview(symbolLabel)
        symbolLabel.backgroundColor = .orange
        view.addSubview(valueLabel)
        valueLabel.backgroundColor = .systemPurple
    }
    
    override func viewDidLayoutSubviews() {
        super.viewDidLayoutSubviews()
        nameLabel.frame = CGRect(x: 0, y: 50, width: view.frame.size.width, height: 30)
        symbolLabel.frame = CGRect(x: 10, y: 90, width: view.frame.size.width, height: 30)
        valueLabel.frame = CGRect(x: 10, y: 130, width: view.frame.size.width, height: 30)

    }
    
    
}
*/
